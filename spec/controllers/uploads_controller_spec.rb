require 'spec_helper'

describe UploadsController do

  #Delete this example and add some real ones
  it "should use UploadsController" do
    controller.should be_an_instance_of(UploadsController)
  end

  it "finds the upload for show"
  it "gets a new upload for new"
  it "updates the title of an upload and displays json data with update"
  it "munges parameters into model form on create with xhr"
  it "renders json on create with xhr"
  it "adds the uuid from parameters into model params on create without xhr"
  it "redirects to show after successful save on create without xhr"
  it "shows the form with errors again on create without xhr"
  it "renders the errors as json and a message for the fileuploader lib with xhr"

end
